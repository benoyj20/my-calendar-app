package calendar.view;

/**
 * Represents the different visual layouts available in the GUI.
 * The controller and view use this enum to track which screen is currently active.
 */
public enum ViewMode {
  DAY, WEEK, MONTH
}